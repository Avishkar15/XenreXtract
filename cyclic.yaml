version: "1.0"

app:
  name: XenreXtract
  framework: python

build:
  steps:
    - name: Install dependencies
      run: pip install -r requirements.txt

  artifacts:
    - name: app
      path: .

deploy:
  steps:
    - name: Start app
      run: gunicorn main:app -b 0.0.0.0:$PORT --timeout 300